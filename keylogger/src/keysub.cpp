#include <ros/ros.h>
#include <std_msgs/Char.h>

void keyCallback(const std_msgs::Char::ConstPtr &msg) {
	/* TODO: print out key to terminal */
}

int main(int argc, char** argv) {
	/* TODO: initialise node and subscriber */	
	return 0;	
}
